programa test_program;
vars {
    x, y : entero;
    z : flotante;
}
inicio {
    x = 10;
    y = 5;
    si (x > y) haz {
        escribe("x is greater than y");
    } sino haz {
        escribe("x is less than or equal to y");
    }
    mientras (y > 0) haz {
        escribe(y);
        y = y - 1;
    }
    z = x / 2.0;
    escribe("The value of z is:", z);
}
fin